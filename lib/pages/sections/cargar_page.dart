import 'package:flutter/material.dart';

class CargarPage extends StatelessWidget {
  const CargarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
