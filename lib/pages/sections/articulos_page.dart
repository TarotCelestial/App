import 'package:flutter/material.dart';

class ArticulosPage extends StatelessWidget {
  const ArticulosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
